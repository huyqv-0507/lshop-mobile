import 'package:flutter/material.dart';

const kdefaultBorder = 8.0;

class BorderHandler {
  static final BorderRadius brDefaultImageBorder = BorderRadius.only(
      topLeft: Radius.circular(kdefaultBorder),
      topRight: Radius.circular(kdefaultBorder));
}
